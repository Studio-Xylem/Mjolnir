spring.application.name=Mjolnir
# Optional when Application Default Credentials do not infer the Google Cloud project.
firebase.project-id=${mjolnir-34e55:}
app.cors.allowed-origin=${CORS_ALLOWED_ORIGIN:http://localhost:5173}
server.port=${PORT:8080}
