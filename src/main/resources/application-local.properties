# Start with: .\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local
app.auth.mode=local
app.storage.location=${LOCAL_STORAGE_PATH:./data/uploads}
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/mjolnir?createDatabaseIfNotExist=true&serverTimezone=UTC}
spring.datasource.username=${MYSQL_USER:mjolnir}
spring.datasource.password=${MYSQL_PASSWORD:mjolnir}
spring.jpa.hibernate.ddl-auto=update
app.cors.allowed-origin=http://localhost:5173
